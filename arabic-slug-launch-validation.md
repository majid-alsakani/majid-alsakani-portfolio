# Arabic Slug & Text Cleaner — Launch Validation

- تمت معاينة صفحة الأداة العربية محلياً ضمن قالب Signal Noir وباتجاه RTL، مع ظهور حقل النص وخيارات التنظيف والنتائج وبطاقة الربط إلى ملف دليل Qalam.
- أُدخلت العينة: `آثارُ واجهةٍ ـــ عربية!!!` مع تفعيل إزالة التشكيل وتطبيع الحروف والنسخة اللاتينية.
- النتيجة المنظفة كانت: `اثار واجهة عربية!!!`، وslug العربي: `اثار-واجهة-عربية`، كما ظهرت نسخة URL-encoded ونسخة لاتينية اختيارية.
- التقطت طبقة الأحداث الاختيارية `tool_start` و`tool_complete` بمعاملات عامة فقط: اللغة وطول الإدخال ضمن فئة وخيارات الأداة. لم يظهر نص المستخدم أو الناتج ضمن الأحداث.
- تحققت النسخة العامة بعد نشر الالتزام `5771e9e`: أعادت العينة نفسها slug `اثار-واجهة-عربية` ونسخة URL-encoded، مع رسالة نجاح محلية وأحداث تصف اللغة والخيارات فقط.
