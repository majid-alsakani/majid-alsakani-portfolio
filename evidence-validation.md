# Evidence Files validation notes

The archive loads as a distinct Signal Noir evidence index with a readable 15-file ledger, visible record sequencing, project tags and direct links to each available case-study file.

The Joobea detail page continues to load its real project screenshot, metadata, architecture evidence, product screens and source/live links. The Evidence Files layer turns its existing content into an archival cinematic record without removing factual material or substituting visual placeholders.

The new contact page loads as a Signal Noir intake path with four staged labels, a visible idea-to-system method card and a first choice field rather than a generic static contact form. The form has not been submitted during validation, so no message was sent to the configured recipient.

The flow was tested without submitting the form: selecting a product type and advancing the first stage correctly activated stage 02, exposed the constraint field and updated the visual rail. This verifies progressive disclosure without sending data to the form recipient.

## Cinematic overhaul audit

The published contact page has a readable foundation but begins with too much static copy and places the interactive choice grid below the fold. The revised direction should move a decisive system visualization into the first viewport, make the first action instantly apparent, and rely on a compact staged flow rather than an extended editorial lead.

The published case-study archive has coherent typography but functions primarily as a ledger. The revised direction should bring the three primary product systems into the opening scene, compress the remaining entries into a secondary evidence index, and use scroll-driven visual transitions so the archive feels like a film sequence rather than a static directory.
