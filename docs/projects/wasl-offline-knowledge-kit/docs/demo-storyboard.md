# Demo video storyboard

The six-second demo uses the real browser preview and moves from the offline promise toward the searchable knowledge pack.

**Narrative:** a user installs a pack, searches a note, reads it locally, and keeps the content portable through export. The video demonstrates the UX surface; it does not promise automatic sync or freshness without a network.
