# Qalam architecture

Qalam treats Arabic interface quality as a rendering and context problem. The local lab counts script runs, numeric characters, and URL-like content, then recommends explicit direction boundaries and device testing.

## Boundary

The MVP is a signal generator, not a language model, translator, or complete bidi conformance suite. Production adoption would add browser snapshots, font/device matrices, localization fixtures, and native-speaker review.
